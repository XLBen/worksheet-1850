CHIP DecoderSegment201658459 {
    IN A, B, C, D;
    OUT b;
PARTS:
    Not(in=A, out=NotA);
    Not(in=B, out=NotB);
    And(in=NotA, in=NotB, out=ABcomb);
    And(in=A, in=B, out=ABcomb2);
    Not(in=C, out=NotC);
    Not(in=D, out=NotD);
    And(in=NotC, in=NotD, out=CDcomb);
    Or(in=ABcomb, in=ABcomb2, out=OrAB);
    Or(in=OrAB, in=CDcomb, out=b);
}