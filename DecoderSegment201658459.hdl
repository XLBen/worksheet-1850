CHIP DecoderSegment201658459 {
    IN A, B, C, D;
    OUT b;
PARTS:
    Not(in=B,out=NotB);
    Not(in=D,out=NotD);
    
    

    And(a=C,b=NotB,out=NotBandC);
    And(a=NotBandC,b=NotB,out=Bpart);

    And(a=A,b=B,out=Bpart);

    And(a=A,b=C,out=Cpart);

    And(a=NotD,b=C,out=Dpart);

    Or(a=Apart,b=Bpart,out=fina1);
    Or(a=fina1,b=Cpart,out=fina2);
    Or(a=fina2,b=Dpart,out=b);
}