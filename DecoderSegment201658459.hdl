CHIP DecoderSegment201658459 {
    IN A, B, C, D;
    OUT b;
PARTS:
    Not(in=B,out=NotB);
    Not(in=D,out=NotD);
    
    And(a=C,b=NotD,out=Apart);

    And(a=A,b=NotB,out=NotBandA);
    And(a=NotBandA,b=C,out=Bpart);

    And(a=A,b=B,out=Cpart);

    And(a=A,b=NotB,out=AandNOTB);
    And(a=AandNOTB,b=C,out=Dpart);

    Or(a=Apart,b=Bpart,out=fina1);
    Or(a=fina1,b=Cpart,out=fina2);
    Or(a=fina2,b=Dpart,out=b);
}