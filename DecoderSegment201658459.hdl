CHIP DecoderSegment201658459 {
    IN A, B, C, D;
    OUT b;
PARTS:
    Not(in=A, out=NotA);
    Not(in=B, out=NotB);
    And(in=NotA, in=NotB, out=AB_comb);
    And(in=A, in=B, out=AB_comb2);
    Not(in=C, out=NotC);
    Not(in=D, out=NotD);
    And(in=NotC, in=NotD, out=CD_comb);
    Or(in=AB_comb, in=AB_comb2, out=Or_AB);
    Or(in=Or_AB, in=CD_comb, out=b);
}