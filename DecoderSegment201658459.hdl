CHIP DecoderSegment201658459 {
    IN A, B, C, D;
    OUT b;
PARTS:
    Not(in=A,out=NotA);
    Not(in=B,out=NotB);
    Not(in=C,out=NotC);
    Not(in=D,out=NotD);
    
    And(a=C,b=D,out=Apart);

    And(a=NotA,b=NotB,out=NotAB);
    And(a=NotAB,b=NotD,out=Bpart);

    And(a=A,b=B,out=Cpart);

    And(a=A,b=C,out=Dpart);

    Or(a=Apart,b=Bpart,out=fina1);
    Or(a=Dpart,b=Cpart,out=fina2);
    Or(a=fina1,b=fina2,out=b);
}